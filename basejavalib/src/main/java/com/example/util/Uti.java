package com.example.util;import java.io.File;import java.io.FileInputStream;import java.math.BigInteger;import java.security.MessageDigest;/** * author: moon * created on: 18/1/25 上午11:27 * description: */public class Uti {    public static void main(String[] args) {        File file = new File("/Users/moon/Desktop/AndResGuard-1.2.11/tool_output/hongyuanyun_release_doingnow_v3.4.2/hongyuanyun_release_doingnow_v3.4.2.apk");        String s = getFileSha1(file);        System.out.println(s);        System.out.println(s.length());        String a = "2018-03-08 12:00:00";        String b  = "2018-03-08 13:48:00";        System.out.println(getCourseDuring(a,b));//        String partPort = "11234567";//        System.out.println(isPassPort(partPort));    }    public static String getCourseDuring(String begin,String end){        try {            String ymd = begin.substring(0,10);            String dur = begin.substring(11,16) + "-"+ end.substring(11,16);            return ymd + " " +dur;        }catch (Exception e){            e.printStackTrace();        }        return "";    }    /**     * 校验护照     *     * @param value     * @return     */    public static boolean isPassPort(String value) {        if (value == null || "".equals(value)){            return false;        }        if (value.length() <8 || value.length() > 9){            return false;        }        if (!Character.isLetter(value.charAt(0))){            return false;        }        boolean isDigit = true;        for (int i = 1; i < value.length(); i++) {            if (!Character.isDigit(value.charAt(i))) {   //用char包装类中的判断数字的方法判断每一个字符                isDigit = false;            }        }        return isDigit;    }    /**     * 获取文件 sha1 值     *     * @param file     * @return s     */    public static String getFileSha1(File file) {        if (!file.isFile()) {            return null;        }        MessageDigest digest = null;        FileInputStream in = null;        byte buffer[] = new byte[8192];        int len;        try {            digest = MessageDigest.getInstance("SHA-1");            in = new FileInputStream(file);            while ((len = in.read(buffer)) != -1) {                digest.update(buffer, 0, len);            }            BigInteger bigInt = new BigInteger(1, digest.digest());            String sha1Value = bigInt.toString(16);            int length = sha1Value.length();            if (length == 40){                return sha1Value;            }else {                int last = 40-length;                StringBuilder sb = new StringBuilder();                for (int i=0;i<last;i++){                    sb.append("0");                }                return sb.toString()+sha1Value;            }        } catch (Exception e) {            e.printStackTrace();            return null;        } finally {            try {                in.close();            } catch (Exception e) {                e.printStackTrace();            }        }    }}